server:
  port: 9009
spring:
  application:
    name: cloud-alibaba-config9009
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml
        group: DEV_GROUP
        namespace: e0fbcbe0-28b0-4514-9e9f-1bab7069701f
#        group: TEST_GROUP
management:
  endpoints:
    web:
      exposure:
        include: "*"

#   cloud-alibaba-config9009-dev.yaml
